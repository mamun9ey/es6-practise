const name = "shahrukh khan";
console.log(name);



// We can not do this because we can not change the value of const variable.
// name = 'Amir Khan';
// console.log(name);


// We can add element to const variable.

const num = [12, 33, 15];
console.log(num);

num.push(23);
console.log(num);

// We can change the value but not entirely. Only the position

num[2] = 44;
console.log(num);



// Let variable.

let patientName = "Rafiq chacha";
console.log(patientName);

patientName = "abudr Rahman";
console.log(patientName);

// We can change value using let variable.